class Solution {
    public boolean isPalindrome(int x) {
        int reverse=0;
        int temp=x;
        while(temp>0){
            int dig=temp%10;
            reverse=reverse*10+dig;
            temp/=10;
        }
        return reverse==x;
        
    }
}
