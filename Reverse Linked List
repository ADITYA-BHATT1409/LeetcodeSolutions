class Solution {
    public ListNode reverseList(ListNode head) {
        ListNode curr=head;
        ListNode n=null;
        ListNode p=null;
        while(curr!=null){
            n=curr.next;
            curr.next=p;
            p=curr;
            curr=n;
        }
        return p;
        
    }
}
