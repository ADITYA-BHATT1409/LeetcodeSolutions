class Solution {
    public int lengthOfLastWord(String s) {
        String temp=s.trim();
        String word[]=temp.split(" ");
        int n=word.length;
        return word[n-1].length();
        
    }
}
